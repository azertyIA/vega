We have done normal probability thus far, but it definitely needs to be more fine for us to do fun math, so we'll just change one axiom:
$$\Sigma \mathbb P[x]=1\to\int dx [\mathbb P[x]] = 1$$
$$\mathbb E[x]=\int dx[xP_X(x)]$$
$$Var[x]=\int dx[x^2P_X(x)]-(\int dx[xP_X(x)])^2$$
