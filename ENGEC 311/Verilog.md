You don't have registers in continuous assignment. And then you just have a ton of assign statements. No ifs.

In procedural, you have to define touchables as registers. You also have to use if statements and switches etc.